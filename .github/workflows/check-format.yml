name: Check source code formatting

on:
  push: {}
  pull_request: {}

jobs:
  check:
    runs-on: ubuntu-latest
    container: zhongruoyu/llvm-ports:19-slim-focal
    steps:
      - uses: actions/checkout@v4
      - name: Check .cpp and .hpp files
        run: |
          ./check_format.sh
